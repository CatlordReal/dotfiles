# Ricing TODO

- Investigate quick smooth window/workspace animations. AeroSpace 0.20.3 does not expose a native animation duration/transition setting, so this likely needs an external animation-capable layer, an upstream AeroSpace feature, or a different WM strategy.
- Keep SketchyBar colours synced with Neovim `<leader>cc` theme changes through `~/.config/ricing/theme` and `~/.config/sketchybar/colors.lua`.
